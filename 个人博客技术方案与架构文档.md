# 个人博客技术方案与架构文档

> 目标：打造个人 IP、内容引流；读者覆盖国内 + 海外
> 架构选型：Next.js + OpenNext + Cloudflare Workers（方案 B）
> 日期：2026-09-07

---

## 1. 总体架构

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  内容生产层  │ ──▶ │   构建与发布层    │ ──▶ │    分发与接入层  │
│             │     │                  │     │                 │
│ Markdown/MDX│     │ Next.js (SSG/ISR)│     │ Cloudflare Edge │
│ ai-infra 报告│     │ OpenNext 适配     │     │ + 国内 CDN 镜像  │
│ 建站笔记     │     │ GitHub Actions   │     │                 │
└─────────────┘     └──────────────────┘     └─────────────────┘
                             │
                    ┌────────▼────────┐
                    │   数据与观测层    │
                    │ Umami（自托管）   │
                    │ Newsletter 订阅   │
                    │ RSS / Sitemap    │
                    └─────────────────┘
```

核心原则：**基础设施隐形，内容管线优先**。博客是内容系统的展示层，不是玩具项目——但既然你选择了 B，我们把"维护过程"本身转化为内容资产（建站笔记分类）。

---

## 2. 技术栈明细

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | Next.js 15 (App Router) | 文章页全量 SSG，列表页 ISR |
| 适配 | OpenNext for Cloudflare | 将 Next.js 构建产物转为 Workers 格式 |
| 托管 | Cloudflare Workers | 海外入口，免费额度个人博客够用 |
| 国内镜像 | 见 §4 双轨部署 | 国内访问优化 |
| 样式 | Tailwind CSS 4 + CSS 变量主题 | 参考 qiaomu.ai 的多主题系统 |
| 内容 | MDX（本地文件，Git 管理） | 不引入 CMS，保持 Git 工作流 |
| 字体 | Geist / Geist Mono + 思源宋体/黑体 | 中英混排优化 |
| 图标 | Lucide React | |
| 分析 | Umami（自托管，沿用现有） | 隐私友好，可加 `data-website-id` 双实例区分国内外流量 |
| 邮件订阅 | Buttondown 或 Listmonk 自托管 | Newsletter 是 IP 沉淀的核心 |
| 评论（可选） | Giscus（GitHub Discussions） | 开发者读者友好，零成本 |
| CI/CD | GitHub Actions → wrangler deploy | push main 自动发布 |

---

## 3. 内容管线设计

本方案有**两条平等的内容管线**，都汇入同一个 `content/posts/` 目录，统一走 Git → CI 发布：

```
管线 1（人工写作）：Obsidian ──────────────┐
                                            │
管线 2（自动化）：ai-infra HTML 报告          │
        │  (脚本: scripts/html-to-mdx.py)    │
        ▼  转换为 MDX + frontmatter          │
                                            │
        ┌───────────────────────────────────┘
        ▼
   content/posts/*.mdx  ← 本身就是 Obsidian Vault（或其子文件夹）
        │
        ▼  (git push → GitHub Actions)
   构建时 remark 管线：wiki-link 转换、图片改写、callout 渲染
        │
        ▼
   next build → opennextjs-cloudflare build → wrangler deploy
        │
        ▼
   Cloudflare Workers（海外） + 同步脚本（国内镜像）
```

### 3.1 Obsidian 写作管线（日常最高频路径）

核心设计：**`content/` 目录直接作为 Obsidian Vault**，写作即编辑源码，发布即 git push。不需要任何"导出"动作。

**Obsidian ↔ MDX 兼容层：**

| Obsidian 习惯 | 博客构建需要 | 处理方式 |
|---|---|---|
| `[[双链]]` | 标准链接 | remark 插件按 slug 映射转换；无对应文章时降级为纯文本 |
| `![[图片.png]]` 附件 | `/images/xxx.png` | Vault 内统一存 `attachments/`，同步脚本搬运到 `public/images/` 并改写路径 |
| 文件夹组织 | frontmatter category | Templater 插件新建文章时自动按文件夹填入 category |
| 未完成的草稿 | 不发布 | 模板默认 `draft: true`，改为 `false` 才上线 |
| Callout（`> [!note]`） | 渲染为组件 | remark 插件支持，样式与主题系统联动 |
| Dataview / 查询块 | 忽略 | 构建时剥离，不进入输出 |

**Obsidian 侧推荐插件配置：**

| 插件 | 用途 |
|---|---|
| Templater | 新文章模板：自动填入 frontmatter 骨架（含日期、默认 draft） |
| Obsidian Git | 定时自动 commit + push，写作完即发布，零手动操作 |
| Linter | 保存时统一 frontmatter 格式、校验必填字段（description 等） |
| Image Converter（可选） | 粘贴图片自动转 WebP 并重命名，直接优化体积 |

**防坑规则：**
- `description` 为必填（Linter 校验），缺失时构建报错——它是 SEO 的命脉
- 文件名即 slug 来源：用英文短横线命名文件（`claude-code-tutorial.mdx`），Templater 可强制提示
- ai-infra 转换产出的文章与 Obsidian 手写文同目录共存，靠 frontmatter 的 `source: ai-infra` 字段区分来源（可选，便于统计）

**Frontmatter 规范：**

```yaml
---
title: 文章标题
slug: claude-code-tutorial        # 语义化 slug，不用日期+ID
date: 2026-09-07
category: ai-tools                # 初期仅 3 个分类
tags: [claude, cli, 效率]
description: 120 字以内的 SEO 描述   # 必填，用于 meta/OG
draft: false
---
```

**分类策略（初期收敛到 3 个）：**

| 分类 | slug | 定位 |
|---|---|---|
| AI 工具 | ai-tools | 工具评测、使用技巧（引流主力，搜索需求大） |
| AI 教程 | ai-tutorials | 深度教程（建立专业度） |
| 建站笔记 | building | 本博客的技术实现过程（技术感人设） |

原有 7 个分类（AI生成/AI资讯/健脑房/播客解读/论文学习）先以 tag 形式存在，待内容量上来（单 tag > 10 篇）再升级为分类。

---

## 4. 双轨部署方案（国内 + 海外）

Cloudflare 在国内访问不稳定，"两者都要"的务实方案：

### 海外主线（主站）
- Workers + 主域名 `liigo.dev`
- Google Search Console 提交 sitemap

### 国内镜像（备案后）
两个选项，按成本排序：

1. **静态镜像**（推荐起步）：构建时额外产出纯静态版本（`next build` 全 SSG 模式），同步到国内对象存储 + CDN（腾讯云 COS + CDN / 阿里 OSS）。文章站交互少，静态版损失极小。使用独立已备案域名（`liigo.cn`）+ `hreflang` 互指。
2. **国内 Serverless**：腾讯云 EdgeOne Pages，可跑同一套 OpenNext 产物，无需双版本，但需备案且有成本。

> ⚠️ 前提：国内分发必须 ICP 备案。未备案前，国内读者只能走主站直连（Cloudflare 速度不可控）。建议尽快启动备案流程（周期 2-4 周）。

### SEO 双轨要点
- 两个域名互为 `rel="alternate" hreflang`
- 或在备案完成前只运营主站，避免权重分散——**推荐后者**：先单点打透，再镜像

---

## 5. 域名规划（已定）

**主域名：liigo.dev**（2026-09 已注册，Cloudflare Registrar）

品牌故事：liigo 源自真名"李果"的拼音缩写变体（Li + Go，双 i 增强辨识度），外人看来是独立品牌词，自己人知道暗线关联。Go 同时双关"行动力"与 Go 语言。

**域名结构：**
- 主站：`liigo.dev`（博客，裸域直接做门面）
- 实验室：`lab.liigo.dev`（现有 portal 迁入，与博客互链）
- 分析：`umami.liigo.dev`（Umami 自托管）
- 国内镜像（备案后）：`liigo.cn`（阿里云注册 + 备案，状态：待启动）

**社交账号统一：** `@liigo` 或 `@liigodev`（X、即刻、公众号、GitHub 逐个占坑）

注册商策略：主域名 Cloudflare Registrar（成本价续费 + 与 Workers 零配置打通）；.cn 域名在阿里云注册便于备案。

---

## 6. SEO 与引流清单（方案 B 下的落地项）

- [ ] 每篇文章 SSG 输出完整 `<title>` / meta description / OG / Twitter Card
- [ ] `/sitemap.xml`、`/feed.xml`（RSS）、`/robots.txt` 自动生成
- [ ] 语义化 slug，URL 中不含日期
- [ ] 结构化数据：`Article` JSON-LD（标题/作者/发布时间/封面）
- [ ] 每篇文章底部：Newsletter 订阅框 + 相关文章推荐（内链）
- [ ] 图片 AVIF/WebP + `next/image` 懒加载，Core Web Vitals 全绿
- [ ] 提交 Google Search Console；备案后提交百度站长
- [ ] RSS 分发：即刻、V2EX、掘金专栏同步（初期手动，每周 30 分钟）

**引流闭环：** 搜索引擎/RSS → 文章 → Newsletter 订阅 → 回访。社交平台（X、即刻、公众号）只做分发入口，不做内容主阵地。

---

## 7. 里程碑计划

| 阶段 | 内容 | 周期 |
|---|---|---|
| M1 骨架 | Next.js + Tailwind + MDX 渲染 + 单主题，本地可跑 | 2-3 天 |
| M2 上线 | OpenNext 适配 + Workers 部署 + 域名 + sitemap/RSS | 2 天 |
| M3 内容迁移 | html-to-mdx 转换脚本 + Obsidian Vault 接入（Templater/Git/Linter 配置 + remark 兼容层）+ 首批 5-10 篇文章 | 3-5 天 |
| M4 增长件 | Newsletter、相关文章、Umami 接入、GSC 提交 | 2 天 |
| M5 国内镜像 | 备案 + 静态镜像同步（**明确后置**：主站运营稳定后再启动，暂不注册 liigo.cn、不备案） | 视主站运营情况而定 |
| 持续 | 主题系统（多主题切换）、搜索、评论——作为建站笔记素材逐步加 | 持续 |

---

## 8. 成本估算

| 项 | 费用 |
|---|---|
| Cloudflare Workers/Pages | 免费额度内（10 万请求/日） |
| 域名 | ~$10-15/年（.com） |
| Umami 自托管 | 已有，0 |
| Newsletter（Buttondown） | 免费档 < 100 订阅 |
| 国内 CDN 镜像（M5） | ~￥30-100/月 |
| **合计（M1-M4）** | **仅域名费用** |

---

## 9. 风险与应对

| 风险 | 应对 |
|---|---|
| OpenNext 适配坑多（Node API 兼容、缓存行为） | 文章站全 SSG 避开运行时问题；坑本身写成建站笔记 |
| Cloudflare 国内不稳定 | M5 国内镜像；备案尽早启动 |
| 双域名权重分散 | 备案前只运营主站，镜像上线后 hreflang |
| 分类扩张过快 | 守住 3 分类红线，tag 先行 |
| 完美主义拖延 | 按里程碑 M1-M4 快速上线，功能迭代作为内容产出 |

---

## 下一步

- [x] 品牌词确定：liigo → 域名 liigo.dev 已注册 ✅
- [x] M1 骨架：Next.js + Tailwind + MDX 渲染 ✅（2026-09-07 完成，预览版已保存）
- [ ] M2 上线：OpenNext 适配 + Workers 部署 + liigo.dev 绑定 + sitemap/RSS
- [ ] 社交账号占坑：@liigo / @liigodev
- [ ] ~~启动 liigo.cn 注册与 ICP 备案~~（决定后置：主站运营稳定后再考虑）
