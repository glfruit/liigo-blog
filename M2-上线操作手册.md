# M2 上线操作手册

> 目标：把 liigo.dev 博客部署到 Cloudflare Workers 并绑定域名
> 前置：已完成 M1（博客骨架 + logo + 日期修复），代码已推送到 GitHub

---

## 一、Cloudflare 侧配置（一次性）

### 1.1 获取 API 凭证

1. 登录 https://dash.cloudflare.com
2. 右上角头像 → **My Profile** → **API Tokens** → **Create Token**
3. 用 **Edit Cloudflare Workers** 模板，权限保持默认
4. Account Resources 选你的账号，**Create Token**，复制保存（只显示一次）

记下两个值：

| 值 | 获取位置 |
|---|---|
| `CLOUDFLARE_API_TOKEN` | 上一步创建 |
| `CLOUDFLARE_ACCOUNT_ID` | Dashboard 右侧栏 / Workers 概览页 URL 中 |

### 1.2 把凭证写进 GitHub Secrets

仓库 → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**，添加：

- `CLOUDFLARE_API_TOKEN`
- `CLOUDFLARE_ACCOUNT_ID`

CI 工作流 `.github/workflows/deploy.yml` 已引用这两个 secret，push main 即自动部署。

---

## 二、首次部署（本地手动跑一次，验证链路）

> ⚠️ 注意：当前开发沙箱不支持符号链接，`npm run deploy` 在沙箱内无法完成。
> 请在**你自己的电脑**（mac-studio 或任意能正常建符号链接的机器）上执行：

```bash
git clone <你的仓库>
cd liigo-blog
npm install

# 配置 wrangler 认证（二选一）
export CLOUDFLARE_API_TOKEN="你的token"
# 或交互式登录：npx wrangler login

# 一键构建 + 部署
npm run deploy
```

成功后 wrangler 会输出默认地址：`liigo-blog.<你的子域>.workers.dev`，先打开验证站点正常。

---

## 三、绑定 liigo.dev 域名

域名在 Cloudflare Registrar，DNS 已自动托管，绑定最简单：

1. Cloudflare Dashboard → **Workers & Pages** → 选中 `liigo-blog`
2. **Settings** → **Domains & Routes** → **Add** → **Custom Domain**
3. 输入 `liigo.dev`，确认。Cloudflare 自动完成 DNS + 证书，几分钟生效
4. （可选）再加 `www.liigo.dev` 并做 301 到裸域

验证：

```bash
curl -I https://liigo.dev
# 应返回 200，server: cloudflare，cf-ray 显示就近节点
```

---

## 四、SEO 提交（上线后立即做）

### 4.1 Google Search Console

1. https://search.google.com/search-console → **添加资源** → 选"网址前缀"，填 `https://liigo.dev`
2. 验证所有权：推荐 **HTML 标记**方式——把验证码贴到 `app/layout.tsx` 的 `<head>` metadata 里，部署后即通过
3. 验证后：**站点地图** → 提交 `https://liigo.dev/sitemap.xml`
4. 用 **网址检查** 工具对 3 篇文章逐个"请求编入索引"，加速首次收录

### 4.2 Bing Webmaster Tools（顺手）

https://www.bing.com/webmasters → 可直接**从 GSC 导入**，30 秒完成。

### 4.3 上线前自检清单

- [ ] `https://liigo.dev/feed.xml` 返回 RSS XML
- [ ] `https://liigo.dev/sitemap.xml` 列出全部页面
- [ ] `https://liigo.dev/robots.txt` 指向 sitemap
- [ ] 任一文章页查看源代码，有 `application/ld+json` 结构化数据
- [ ] 任一文章页"查看 OG 标签"：复制 URL 到 https://www.opengraph.xyz 检查分享卡片
- [ ] favicon 在浏览器标签页显示徽章 logo

---

## 五、日常发布流（上线后）

```
Obsidian 写作（draft: true）
    ↓  Obsidian Git 自动同步
改为 draft: false
    ↓  git push main
GitHub Actions 自动构建 + 部署到 Workers
    ↓  约 1-2 分钟
liigo.dev 上线
```

无需任何手动部署操作。

---

## 六、故障排查

| 现象 | 排查 |
|---|---|
| `npm run deploy` 报符号链接错误 | 你在沙箱/无 symlink 权限环境，换本机跑 |
| Workers 域名 404 | Custom Domain 未生效，等 5 分钟或检查 DNS |
| 文章页 404 | 检查 frontmatter `draft` 是否为 `true` |
| RSS/sitemap 404 | 确认构建产物包含 `dist/feed.xml`，静态导出正常 |
| CI 部署失败 | 检查两个 secret 是否正确、API Token 权限是否含 Workers 编辑 |

---

## 七、成本确认

- Cloudflare Workers：免费档 10 万请求/日，个人博客用不完
- liigo.dev 域名：已付，续费约 $12/年
- GitHub Actions：公开仓库免费
- **当前总成本：仅域名续费**
