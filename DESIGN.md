# DESIGN.md — liigo.dev 视觉规范

> 品牌契约：任何会话/agent 为这个博客做页面、插图、组件时，以此为准。
> 格式参考 open-design 的 DESIGN.md 约定。

## 1. 设计哲学

**单色墨纸（Ink on Paper）**——整个博客只有黑与白之间的灰度，像一份印在纸上的独立刊物。
克制、目录式、档案感。所有视觉决策的试金石：这个东西印在纸上好看吗？

## 2. 色彩

| 角色 | 值 | 用途 |
|---|---|---|
| ink | `#0a0a0a` | 正文、标题、按钮底色 |
| paper | `#fafafa` | 页面背景 |
| ink-3 | `#757575` | 辅助文字、日期、标签 |
| line | `#e5e5e5` | 发丝线分隔 |

**硬性规则**：禁止引入任何彩色。没有品牌蓝、没有强调橙，链接和按钮也不变色。

## 3. 排印

- 标题/签名句：`font-display`（Inter + 思源宋体系），字重 600，字距收紧（tracking-tight）
- 正文：系统无衬线（PingFang SC 等），行高宽松（leading-loose）
- 元信息（日期、标签、导航）：等宽字体 + 全大写/括号装饰，如 `( 目录 / Index )`
- 列表呈现用**目录式排印**：极紧行高、发丝线分隔行、悬停箭头淡入

## 4. 发丝线（Hairline）

- 分隔只用 1px `--color-line`，高 DPI 屏降为 0.5px
- 工具类：`.hairline`（四边）/ `.hairline-b`（底）/ `.hairline-t`（顶），定义在 `app/globals.css`
- 不用阴影、不用卡片底色块来分区——分区靠发丝线和留白

## 5. 插画规范（AI 生成）

场景：关于页、文章封面、空状态等。

**风格**：编辑插画级水墨/钢笔线条，黑色墨线 + 浅灰淡彩，绝无彩色。

**融入页面的两条铁律**：
1. 生成时要求背景为**精确 #fafafa 平面底色**，画面元素四周留 ≥15% 纯空白边距
2. 后处理两道工序（PIL 脚本）：
   - **纸色压平**：与 #fafafa 距离小于阈值（≈24）的像素归位为精确纸色，顺带抹除生成水印
   - **边缘羽化**：四边 60px 高斯渐变带转透明 alpha，消除任何可察觉的矩形边界

**留白原则**：画面宁空勿满。天的辽阔用空白表达，不靠填满。
**构图参考**：已上线的范例 `public/images/about-youth-tianwen.png`（盘腿困惑的中年人 + 思想云里好奇的年轻自己 + 疏朗星月天问氛围）。

**提示词装配顺序**（参考 open-design image-poster 技能）：
主体+构图 → 光影+情绪 → 色彩+肌理（锚定 #fafafa）→ 镜头（仅写实场景需要）→ 负面清单（无文字/无框/无深色块/无渐变底）。

## 6. 图标与 Logo

- 徽章式 logo：墨色圆角方块 + 反白双 i，圆点左高右低错落（`public/icons/logo.svg`）
- UI 图标用 Lucide，等宽细线风格，不加色

## 7. 交互

- 悬停反馈只用透明度/墨色深浅变化（hover:text-ink、hover:opacity-85）
- 不用彩色 toast/高亮；选区样式为灰字透明底（避免浏览器默认蓝）
